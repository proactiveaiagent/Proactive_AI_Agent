org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# AGP 8.x requires JDK 17 for the Gradle daemon. In Android Studio:
# Settings → Build, Execution, Deployment → Build Tools → Gradle → Gradle JDK → 17 (Embedded or installed).
